export class SliderModel {
    img: string;
    name: string;
    url: string;

    constructor(img: string, name: string, url: string) {
        this.img = img;
        this.name = name;
        this.url = url;
    }
}